# tb/system/ — owned by Teammate D

tb_top.v — full-core testbench. Runs actual firmware (from firmware/)
against the assembled core once rtl/top.v exists. This is where the
report's section 6 (Firmware Demo) waveforms and logs come from.
